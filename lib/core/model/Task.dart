
class Task {
  bool isDaily;
  String name;
  DateTime dueDate;
  String description;
  bool done;

  // TaskCategory taskCategory;
  // Meeting meeting;

  Task(this.isDaily, this.name, this.dueDate, this.description, this.done);

}